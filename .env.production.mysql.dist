# Use this file when hosting only the built dist folder with MySQL enabled (copy to: dist/.env)
NODE_ENV=production
PORT=3000
PING_MESSAGE=ping

# MySQL connection (enables persistent CRUD)
# Update these to match your database before deploying
MYSQL_HOST=127.0.0.1
MYSQL_DATABASE=todos
MYSQL_USER=todos
MYSQL_PASSWORD=change_this

# Frontend API base (override if you host your own API)
VITE_API_BASE=https://wph-024-api-todolist.vercel.app
# Server proxy target (optional): if set, backend will proxy /todos & /api/todos to this external API
EXTERNAL_API_BASE=https://backend-todo-api.onrender.com
# Optional extra path prefix on external API (e.g. /api)
EXTERNAL_TODOS_PREFIX=/api
